# Copy to agent_config.yaml (git-ignored) and fill from band.ai/agents.
# Each agent is a separate "External Agent" created on the Band platform.
# The API Key is shown ONCE in a popup on creation — copy it immediately.

coordinator:
  agent_id: "<coordinator-agent-uuid>"
  api_key: "<coordinator-api-key>"

legal:
  agent_id: "<legal-agent-uuid>"
  api_key: "<legal-api-key>"

risk:
  agent_id: "<risk-agent-uuid>"
  api_key: "<risk-api-key>"

finance:
  agent_id: "<finance-agent-uuid>"
  api_key: "<finance-api-key>"

compliance:
  agent_id: "<compliance-agent-uuid>"
  api_key: "<compliance-api-key>"
